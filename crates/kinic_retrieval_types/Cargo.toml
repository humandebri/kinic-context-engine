[package]
name = "kinic_retrieval_types"
version = "0.1.0"
edition = "2024"
license = "MIT"
repository = "https://github.com/ICME-Lab/kinic-context-engine"
description = "Shared retrieval request and result types for the KINIC context runtime."

[dependencies]
candid = "0.10.20"
serde = { version = "1.0.228", features = ["derive"] }
